rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tit_apps/traktor_indonesia_trading/data/main {
      allow read, write: if request.auth != null;
    }
  }
}
